# Руководство пользователя - Network Scanner

## Содержание

1. [Введение](#введение)
2. [Установка](#установка)
3. [Быстрый старт](#быстрый-старт)
4. [Основные функции](#основные-функции)
5. [Параметры командной строки](#параметры-командной-строки)
6. [GUI: работа с результатами](#gui-работа-с-результатами)
7. [Примеры использования](#примеры-использования)
8. [Понимание результатов](#понимание-результатов)
9. [Устранение неполадок](#устранение-неполадок)
10. [Часто задаваемые вопросы](#часто-задаваемые-вопросы)

---

## Введение

**Network Scanner** — это кроссплатформенная утилита для сканирования локальных сетей (CLI и GUI). Она помогает обнаруживать устройства в сети, определять открытые порты, протоколы и типы устройств.

### Основные возможности

- 🔍 **Автоматическое определение сети** — утилита сама находит вашу локальную сеть
- 📡 **Обнаружение устройств** — находит все активные устройства в сети
- 🔌 **Сканирование портов** — проверяет открытые TCP порты на устройствах
- 🖥️ **Определение типов устройств** — автоматически определяет тип устройства
- 📊 **Детальная аналитика** — статистика по протоколам, портам и устройствам
- 🏷️ **Определение производителя** — показывает производителя по MAC адресу
- 🖼️ **GUI-режимы результатов** — `Таблица` / `Карточки` с сохранением выбранного режима

---

## Установка

### Требования

- **Операционная система:** Windows, macOS, Linux (любая архитектура)
- **Go:** версия 1.24 или выше (только для сборки из исходников)
- **Права:** для получения MAC адресов может потребоваться запуск с правами администратора

### Варианты установки

#### Вариант 1: Сборка из исходников (рекомендуется)

1. **Установите Go:**
   ```bash
   # macOS через Homebrew
   brew install go
   
   # Или скачайте с https://go.dev/dl/
   ```

2. **Клонируйте или скачайте проект**

3. **Соберите приложение:**
   ```bash
   cd "Сканер локальной сети"
   go mod download
   go build -o network-scanner ./cmd/network-scanner
   ```

#### Вариант 2: Использование готовых бинарников

Если доступны готовые бинарники для вашей платформы, просто скачайте и запустите.

---

## Быстрый старт

### Самый простой способ

```bash
# Запустите утилиту без параметров
./network-scanner
```

Утилита автоматически:
1. Определит вашу локальную сеть
2. Найдет все активные устройства
3. Просканирует порты 1-1000 на каждом устройстве
4. Выведет результаты в виде таблицы

### Пример вывода

```
Автоматически определена сеть: 192.168.1.0/24
Начинаю сканирование сети...
Сканирование 254 хостов, порты: 1000
Проверка доступности хостов...
Проверено хостов: 254/254, найдено активных: 12
Найдено 12 активных хостов
Сканирование портов...
Сканирование портов: 12/12 хостов
Сканирование завершено

====================================================================================================
РЕЗУЛЬТАТЫ СКАНИРОВАНИЯ СЕТИ
====================================================================================================

┌──────────────┬──────────────────┬──────────┬──────────────┬────────────┬──────────────────┬─────────────┐
│ IP           │ MAC              │ Hostname │ Порты        │ Протоколы  │ Тип устройства  │ Производитель│
├──────────────┼──────────────────┼──────────┼──────────────┼────────────┼──────────────────┼─────────────┤
│ 192.168.1.1 │ aa:bb:cc:dd:ee:ff│ router   │ 80/tcp (HTTP)│ HTTP       │ Router/Network   │ -           │
│              │                  │          │ 443/tcp (HTTPS)│ HTTPS     │ Device          │             │
└──────────────┴──────────────────┴──────────┴──────────────┴────────────┴──────────────────┴─────────────┘
```

---

## Основные функции

### 1. Автоматическое определение сети

Утилита автоматически находит вашу локальную сеть, анализируя активные сетевые интерфейсы.

**Когда использовать вручную:**
- Если у вас несколько сетевых интерфейсов
- Если нужно просканировать другую сеть
- Если автоматическое определение не работает

### 2. Обнаружение активных хостов

Утилита проверяет доступность хостов, пытаясь подключиться к популярным портам:
- 80 (HTTP)
- 443 (HTTPS)
- 22 (SSH)
- 135, 139, 445 (Windows)

### 3. Сканирование портов

Проверяет открытые TCP порты на обнаруженных устройствах. По умолчанию сканирует порты 1-1000.

### 4. Определение типов устройств

Автоматически определяет тип устройства на основе:
- Открытых портов
- Используемых протоколов
- MAC адреса (OUI)

**Поддерживаемые типы:**
- Router/Network Device
- Web Server
- Database Server
- Windows Computer
- Linux/Unix Server
- Printer
- IoT Device

### 5. Аналитика сети

После сканирования выводится детальная статистика:
- Протоколы в сети и их описание
- Используемые порты и их назначение
- Типы устройств
- Общая статистика

---

## Параметры командной строки

### Основные параметры

| Параметр | Описание | По умолчанию | Пример |
|----------|----------|-------------|--------|
| `--network` | Диапазон сети для сканирования (CIDR) | Автоопределение | `--network 192.168.1.0/24` |
| `--ports` | Порты для сканирования | `1-1000` | `--ports 80,443,8080` |
| `--timeout` | Таймаут TCP/UDP в секундах | `2` | `--timeout 5` |
| `--threads` | Количество потоков | `50` | `--threads 200` |
| `--show-closed` | Показывать закрытые порты | `false` | `--show-closed` |
| `--udp` | Включить UDP-сканирование | `false` | `--udp` |
| `--topology` | Построить топологию после сканирования | `false` | `--topology` |
| `--output-format` | Формат экспорта топологии | пусто | `--output-format json` |
| `--output-file` | Файл для экспорта топологии | авто по формату | `--output-file topology.json` |
| `--snmp-community` | SNMP community (через запятую) | `public` | `--snmp-community public,private` |
| `--snmp-timeout` | Таймаут SNMP в секундах | `2` | `--snmp-timeout 4` |

### Инструменты (P1)

| Параметр | Описание | По умолчанию | Пример |
|----------|----------|-------------|--------|
| `--ping` | Ping выбранного хоста/IP и выход | пусто | `--ping 8.8.8.8` |
| `--traceroute` | Traceroute/tracert выбранного хоста/IP и выход | пусто | `--traceroute google.com` |
| `--dns` | DNS lookup (A/AAAA или PTR) и выход | пусто | `--dns 1.1.1.1` |
| `--whois` | Whois lookup (домен/IP) и выход | пусто | `--whois example.com` |
| `--wifi` | Показать Wi-Fi информацию текущей ОС и выход | `false` | `--wifi` |
| `--dns-server` | Кастомный DNS сервер для `--dns` (`IP` или `IP:port`) | системный | `--dns-server 1.1.1.1:53` |
| `--ping-count` | Количество ping-пакетов для `--ping` (1..50) | `4` | `--ping-count 6` |
| `--tool-timeout` | Таймаут tool-режимов (`ping/traceroute/dns/whois/wifi`) в секундах | `60` | `--tool-timeout 20` |
| `--traceroute-max-hops` | Максимальное число hop для `--traceroute` (1..64) | `30` | `--traceroute-max-hops 20` |
| `--raw` | Печатать полный сырой вывод вместе со сводкой | `false` | `--ping 8.8.8.8 --raw` |
| `--audit-open-ports` | После сканирования выполнить базовый аудит открытых портов | `false` | `--audit-open-ports` |
| `--audit-min-severity` | Минимальная критичность для `--audit-open-ports` (`all|low|medium|high|critical`) | `low` | `--audit-min-severity high` |

### Инструменты (P2)

| Параметр | Описание | По умолчанию | Пример |
|----------|----------|-------------|--------|
| `--grab-banners` | Собирать баннеры/версии сервисов с типовых TCP-портов | `false` | `--grab-banners` |
| `--show-raw-banners` | Печатать сырой banner в колонке портов (вместе с `version`) | `false` | `--grab-banners --show-raw-banners` |
| `--verbose-port-logs` | Включить детальные debug-логи по каждому probe порта (шумно, медленнее) | `false` | `--verbose-port-logs` |
| `--wol-mac` | Отправить Wake-on-LAN magic packet на MAC и завершить | пусто | `--wol-mac aa:bb:cc:dd:ee:ff` |
| `--wol-broadcast` | Broadcast для `--wol-mac` (`IP` или `IP:port`) | `255.255.255.255:9` | `--wol-broadcast 192.168.1.255:9` |
| `--wol-iface` | Интерфейс для `--wol-mac` при автоподборе broadcast | пусто | `--wol-iface eth0` |
| `--os-detect-active` | Включить расширенные (active) эвристики определения ОС | `false` | `--os-detect-active` |
| `--risk-signatures` | После сканирования выполнить локальные сигнатуры "домашних" рисков | `false` | `--risk-signatures` |
| `--device-action` | Управление оборудованием: `status`/`reboot` (tool-режим и выход) | пусто | `--device-action status` |
| `--device-target` | Целевой URL API устройства | пусто | `--device-target http://192.168.1.1` |
| `--device-vendor` | Профиль API устройства: `generic-http`/`tp-link-http` | `generic-http` | `--device-vendor tp-link-http` |
| `--device-user` | Пользователь для device-control | пусто | `--device-user admin` |
| `--device-pass` | Пароль для device-control | пусто | `--device-pass secret` |
| `--device-confirm` | Подтверждение для опасной операции reboot (`I_UNDERSTAND`) | пусто | `--device-confirm I_UNDERSTAND` |
| `--device-timeout` | Таймаут device-control в секундах | `10` | `--device-timeout 15` |
| `--audit-log` | Путь к JSONL audit log действий device-control | `audit/device-actions.log` | `--audit-log audit/device-actions.log` |

### Инструменты (P3 Remote Exec MVP)

| Параметр | Описание | По умолчанию | Пример |
|----------|----------|-------------|--------|
| `--remote-exec-transport` | Транспорт удаленного выполнения: `ssh`, `wmi`, `winrm` | пусто | `--remote-exec-transport ssh` |
| `--remote-exec-target` | Целевой хост/IP | пусто | `--remote-exec-target 192.168.1.10` |
| `--remote-exec-command` | Команда для выполнения | пусто | `--remote-exec-command "hostname"` |
| `--remote-exec-user` | Пользователь для транспорта | пусто | `--remote-exec-user admin` |
| `--remote-exec-pass` | Пароль (если применимо к транспорту) | пусто | `--remote-exec-pass secret` |
| `--remote-exec-policy-file` | JSON policy с allowlist (`allow_hosts`, `allow_commands`) | пусто | `--remote-exec-policy-file config/remote-exec-policy.example.json` |
| `--remote-exec-policy-strict` | Строгий policy-режим: только policy-файл, inline allowlist запрещен | `false` | `--remote-exec-policy-strict` |
| `--remote-exec-allow-hosts` | Allowlist хостов через запятую | пусто | `--remote-exec-allow-hosts 192.168.1.10` |
| `--remote-exec-allow-commands` | Allowlist команд через запятую | пусто | `--remote-exec-allow-commands "hostname,uname -a"` |
| `--remote-exec-consent` | Явное подтверждение опасной операции | пусто | `--remote-exec-consent I_UNDERSTAND` |
| `--remote-exec-dry-run` | Проверка policy без выполнения команды | `true` | `--remote-exec-dry-run=false` |
| `--remote-exec-timeout` | Таймаут remote exec в секундах | `15` | `--remote-exec-timeout 20` |
| `--remote-exec-audit-log` | Путь к JSONL audit log | `audit/remote-exec.log` | `--remote-exec-audit-log audit/remote-exec.log` |
| `--security-report-redact` | Маскировать чувствительные данные в HTML security report | `true` | `--security-report-redact=false` |
| `--security-report-unsafe-consent` | Подтверждение для unredacted security report | пусто | `--security-report-unsafe-consent I_UNDERSTAND_UNREDACTED_REPORT` |
| `--security-report-file` | Путь к security report или `auto` для имени по redaction-режиму | пусто | `--security-report-file auto` |

Примечание по безопасности:
- В CLI и audit log автоматически маскируются типовые шаблоны секретов (`password`, `token`, `secret`, `api-key`) в тексте ошибок и выводе команды.
- В HTML security report отображается статус режима маскирования: `REDACTION: ON` или `REDACTION: OFF`.
- В HTML security report отображается metadata-блок (`mode`, `policy`, `unsafe-consent`) для аудит-трека.
- При `--security-report-file auto` имя файла включает `report-id` и режим redaction.

### Подробное описание

#### `--network`

Указывает сеть для сканирования в формате CIDR.

**Форматы:**
- `192.168.1.0/24` — подсеть с 254 хостами
- `10.0.0.0/16` — большая подсеть
- `172.16.0.0/24` — другая подсеть

**Примеры:**
```bash
./network-scanner --network 192.168.1.0/24
./network-scanner --network 10.0.0.0/16
```

#### `--ports`

Указывает порты для сканирования. Поддерживает несколько форматов.

**Форматы:**
- Диапазон: `1-1000`
- Список: `80,443,8080`
- Комбинация: `80,443,8080-8090,9000`

**Примеры:**
```bash
# Только популярные порты
./network-scanner --ports 80,443,22,3389

# Диапазон портов
./network-scanner --ports 1-1000

# Комбинация
./network-scanner --ports 80,443,8080-8090
```

#### `--timeout`

Таймаут для попытки подключения к порту.

**Форматы:**
- `1` — 1 секунда
- `2` — 2 секунды
- `5` — 5 секунд

**Примеры:**
```bash
# Быстрое сканирование (может пропустить медленные устройства)
./network-scanner --timeout 1

# Медленное, но более точное сканирование
./network-scanner --timeout 10
```

#### `--threads`

Количество одновременных подключений.

**Рекомендации:**
- Быстрая сеть: 200-500
- Обычная сеть: 50 (по умолчанию)
- Медленная сеть: 20-50

**Примеры:**
```bash
# Быстрое сканирование (больше нагрузка на сеть)
./network-scanner --threads 500

# Медленное сканирование (меньше нагрузка)
./network-scanner --threads 50
```

---

## GUI: работа с результатами

GUI-версия запускается из `cmd/gui` и содержит три ключевые вкладки:

- `Сканирование` — запуск сканирования и просмотр результатов.
- `Топология` — построение и просмотр топологии сети.
- `Инструменты` — запуск `Ping`, `Traceroute`, `DNS lookup`, `Whois`, `Wi-Fi`, `Wake-on-LAN`, `Аудит портов`, `Risk Signatures`, `Device Control`.
- Для `Аудит портов` в GUI доступен селектор `Audit min severity` (`all|low|medium|high|critical`, по умолчанию `low`) с сохранением в `Preferences`.
- Во вкладке `Инструменты` доступен `Operations Center`: история последних операций, выбор операции и действия `Retry`/`Cancel` (в зависимости от статуса).

### Режимы отображения результатов (вкладка `Сканирование`)

Вкладка `Сканирование` поддерживает подрежимы `Devices` и `Security`.

#### Подрежим `Devices`

Доступны два режима отображения:

- `Таблица`:
  - колонки `HostName`, `IP`, `MAC`, `Порты`;
  - порты отображаются в виде чипов с переносом строк;
  - при узком окне таблица остается доступной через горизонтальную прокрутку;
  - под таблицей отображается markdown-сводка аналитики (`Протоколы`, `Типы устройств`).
- `Карточки`:
  - устройства отображаются в виде карточек (`HostName`, `IP`, `MAC`, порты-чипы);
  - на широком экране показывается сетка из нескольких колонок;
  - на узком экране карточки автоматически перестраиваются в одну колонку;
  - аналитика показывается двумя круговыми диаграммами (`Протоколы`, `Типы устройств`).

В `Devices`-подрежиме также доступен `Host Details Drawer`:
- показывает детали выбранного устройства (`Host/IP/MAC/Type/Vendor/OS/SNMP/Open ports`);
- содержит быстрые действия: `Ping`, `Traceroute`, `DNS`, `Whois`, `Wake-on-LAN`.

#### Подрежим `Security`

Показывает агрегированный `Security Dashboard` поверх текущего отфильтрованного набора устройств:
- сводка по findings (`audit + risk signatures`);
- таблица findings (`source`, `severity`, `host`, `title`);
- кнопка `Export security report (HTML)`.

### Сохранение режима

Выбранный режим (`Таблица` или `Карточки`) сохраняется в настройках GUI и автоматически восстанавливается после перезапуска приложения.

Также сохраняются параметры отображения результатов:
- сортировка (`IP`/`HostName`);
- подрежим результатов (`Devices`/`Security`);
- лимит отображаемых чипов портов;
- строка фильтра;
- быстрые фильтры по типам устройств;
- флаг `Только с открытыми портами`.
- фильтр по `CIDR`;
- фильтр по состоянию портов (`Все`, `Есть открытые`, `Есть закрытые`, `Есть фильтруемые`).
- переключатель `Показывать raw banner`.

Дополнительно для процесса сканирования сохраняются:
- флаг `Собирать баннеры/версии служб`;
- флаг `Активные эвристики определения ОС`.
- флаг `Детальные логи по портам (debug, шумно)`.

В GUI у опции детальных логов доступна кнопка `Подробнее` с кратким пояснением, когда этот режим стоит включать (в основном для диагностики).
Также доступна кнопка `Рекомендуемые настройки`, которая применяет безопасный профиль для повседневного сканирования одним кликом и адаптирует глубину/параллелизм под оценку размера подсети. Рядом кнопка `Почему?` показывает краткую логику подбора параметров, а строка `Профиль: ...` отображает примененный класс профиля (`small/medium/large/very-large`).
Класс рекомендованного профиля сохраняется в настройках GUI отдельным параметром и корректно восстанавливается после перезапуска (с fallback на старый текстовый формат для обратной совместимости).

### Практические ограничения

- Для устройств с очень большим числом открытых портов список чипов сокращается, остаток показывается как `+N`.
- Графические диаграммы в карточном режиме кэшируются для ускорения повторной отрисовки.

### Сохранение результатов из GUI

Кнопка `Сохранить результаты` сохраняет именно текущее представление:
- с учетом активных фильтров;
- с учетом выбранной сортировки.

Если после фильтрации список пустой, сохранение не выполняется и показывается информационное сообщение.

---

## Примеры использования

### Быстрая проверка этапов (closure)

```bash
# Linux/macOS (6-command runbook, copy/paste)
./scripts/p1-closure-check.sh && ./scripts/p2-closure-check.sh && ./scripts/p3-closure-check.sh && ./scripts/stage2-p1-closure-check.sh && ./scripts/stage2-p2-closure-check.sh && ./scripts/stage2-p3-closure-check.sh
```

```powershell
# Windows PowerShell (6-command runbook, copy/paste)
.\scripts\p1-closure-check.ps1; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; .\scripts\p2-closure-check.ps1; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; .\scripts\p3-closure-check.ps1; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; .\scripts\stage2-p1-closure-check.ps1; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; .\scripts\stage2-p2-closure-check.ps1; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; .\scripts\stage2-p3-closure-check.ps1
```

```bash
# Linux/macOS
./scripts/p1-closure-check.sh
./scripts/p2-closure-check.sh
./scripts/p3-closure-check.sh
./scripts/stage2-p1-closure-check.sh
./scripts/stage2-p2-closure-check.sh
./scripts/stage2-p3-closure-check.sh
```

```powershell
# Windows PowerShell
.\scripts\p1-closure-check.ps1
.\scripts\p2-closure-check.ps1
.\scripts\p3-closure-check.ps1
.\scripts\stage2-p1-closure-check.ps1
.\scripts\stage2-p2-closure-check.ps1
.\scripts\stage2-p3-closure-check.ps1
```

`p2-closure-check` дополнительно проверяет P2-флаги CLI (`--grab-banners`, `--show-raw-banners`, `--os-detect-active`) и negative-case для WOL с некорректным MAC.
`stage2-p1-closure-check` проверяет tool-режимы `whois/wifi/audit` и включает отдельный CLI e2e-контроль `--whois` через `go test ./cmd/network-scanner -run Whois` (RDAP fallback без внешней сети).
`stage2-p2-closure-check` проверяет `risk-signatures/device-control` и наличие секций `CVE Findings` + `Risk Signature Findings` в security report.
`stage2-p3-closure-check` проверяет guardrails `security-report` (redaction/consent/report-id/auto filename) и `remote-exec` (strict policy/allowlist behavior).
Для CI валидации используйте `scripts/check-ci-status.sh|ps1` и `scripts/trigger-ci-workflow.sh|ps1`: теперь они fail-fast, если jobs `Stage2 P1 Closure` или `Stage2 P3 Closure` не зелёные.

### Пример 1: Базовое сканирование

```bash
./network-scanner
```

Сканирует автоматически определенную сеть с настройками по умолчанию.

### Пример 2: Сканирование конкретной сети

```bash
./network-scanner --network 192.168.0.0/24
```

Сканирует указанную сеть.

### Пример 3: Сканирование только веб-портов

```bash
./network-scanner --ports 80,443,8080,8443
```

Быстрое сканирование только веб-серверов.

### Пример 4: Быстрое сканирование популярных портов

```bash
./network-scanner --ports 22,80,443,3389,3306 --timeout 1 --threads 200
```

Оптимизировано для быстрого обнаружения основных сервисов.

### Пример 5: Детальное сканирование

```bash
./network-scanner --ports 1-65535 --timeout 5 --threads 50
```

Полное сканирование всех портов (может занять много времени).

### Пример 6: Сканирование с выводом закрытых портов

```bash
./network-scanner --show-closed
```

Показывает также закрытые порты (полезно для отладки).

### Пример 7: Построение топологии сети

```bash
./network-scanner --topology
```

Выполняет сканирование и строит топологию, выводя найденные связи в консоль.

### Пример 8: Экспорт топологии в JSON

```bash
./network-scanner --topology --output-format json --output-file topology.json
```

### Пример 9: Экспорт топологии в GraphML

```bash
./network-scanner --topology --output-format graphml --output-file topology.graphml
```

### Пример 10: Экспорт топологии в PNG/SVG

```bash
./network-scanner --topology --output-format png --output-file topology.png
./network-scanner --topology --output-format svg --output-file topology.svg
```

Для PNG/SVG требуется установленный Graphviz (`dot` в `PATH`).

Если `dot` недоступен и запрошен `--output-format png|svg`, CLI не падает: будет сохранен fallback `JSON` с тем же базовым именем файла и выведено диагностическое сообщение.

### Пример 11: Несколько SNMP community и таймаут

```bash
./network-scanner --topology --snmp-community public,private,monitor --snmp-timeout 4
```

### Пример 11.1: Smoke-проверка D-track экспорта топологии

```bash
./scripts/smoke-d-track-topology-export.sh
```

```powershell
.\scripts\smoke-d-track-topology-export.ps1
```

Скрипт проверяет:
- экспорт `json` и `graphml`;
- эквивалентность узлов/связей между `json` и `graphml`;
- экспорт `png` или fallback в `json`, если Graphviz недоступен.

Для ручной проверки импорта `GraphML` в yEd/Gephi используйте:
- `docs/GRAPHML_COMPATIBILITY_CHECK.md`

### Пример 12: UDP-сканирование вместе с TCP

```bash
./network-scanner --udp --ports 1-256 --timeout 2
```

### Пример 13: Ping-режим без сканирования сети

```bash
./network-scanner --ping 8.8.8.8
```

### Пример 14: Traceroute-режим

```bash
./network-scanner --traceroute google.com
```

### Пример 15: DNS lookup через кастомный resolver

```bash
./network-scanner --dns example.com --dns-server 1.1.1.1 --raw
```

### Пример 16: Ping с кастомным count и timeout

```bash
./network-scanner --ping 8.8.8.8 --ping-count 6 --tool-timeout 20
```

### Пример 17: Whois lookup

```bash
./network-scanner --whois example.com --tool-timeout 20
```

Примечание:
- если системная утилита `whois` отсутствует в `PATH`, CLI автоматически использует RDAP fallback;
- в автоматических проверках Stage2/P1 этот путь валидируется отдельным e2e тестом (`WhoisUsesRDAPFallback`).

### Пример 18: Wi-Fi информация текущей ОС

```bash
./network-scanner --wifi --tool-timeout 20
```

Ожидаемый формат вывода:
- сначала краткая структурированная сводка (`SSID`, `BSSID`, `Signal`, `Channel`, `Security`, где доступно),
- затем `Raw output` системной утилиты для диагностики.

### Пример 19: Базовый аудит открытых портов

```bash
./network-scanner --network 192.168.1.0/24 --audit-open-ports
```

Пример фильтрации по критичности:

```bash
./network-scanner --network 192.168.1.0/24 --audit-open-ports --audit-min-severity high
```

Формат отчёта аудита:
- агрегированная сводка (`Всего находок`, `Хостов с рисками`, `Максимальная критичность`, `Risk score`, разбивка по severity);
- список хостов с количеством находок;
- детализированный список рисков с рекомендациями.

### Пример 20: Сканирование с баннерами/версиями сервисов

```bash
./network-scanner --network 192.168.1.0/24 --ports 21,22,25,80,443 --grab-banners
```

### Пример 21: Показ сырого banner в CLI

```bash
./network-scanner --network 192.168.1.0/24 --ports 80,443 --grab-banners --show-raw-banners
```

### Пример 22: Wake-on-LAN (CLI)

```bash
./network-scanner --wol-mac aa:bb:cc:dd:ee:ff --wol-broadcast 192.168.1.255:9
```

### Пример 23: Wake-on-LAN через интерфейс (авто-broadcast)

```bash
./network-scanner --wol-mac aa:bb:cc:dd:ee:ff --wol-iface eth0
```

### Пример 24: Расширенные эвристики определения ОС

```bash
./network-scanner --network 192.168.1.0/24 --os-detect-active
```

### Пример 25: Remote Exec в режиме dry-run через policy file

```bash
./network-scanner \
  --remote-exec-transport ssh \
  --remote-exec-target 192.168.1.10 \
  --remote-exec-user admin \
  --remote-exec-command "hostname" \
  --remote-exec-policy-file config/remote-exec-policy.example.json \
  --remote-exec-policy-strict \
  --remote-exec-consent I_UNDERSTAND \
  --remote-exec-dry-run
```

### Пример 26: Risk Signatures после сканирования

```bash
./network-scanner --network 192.168.1.0/24 --grab-banners --risk-signatures
```

### Пример 27: Device Control status (безопасный сценарий)

```bash
./network-scanner \
  --device-action status \
  --device-target http://192.168.1.1 \
  --device-vendor generic-http \
  --device-user admin \
  --device-pass secret
```

### Пример 28: Device Control reboot (только с явным подтверждением)

```bash
./network-scanner \
  --device-action reboot \
  --device-target http://192.168.1.1 \
  --device-vendor tp-link-http \
  --device-user admin \
  --device-pass secret \
  --device-confirm I_UNDERSTAND \
  --audit-log audit/device-actions.log
```

---

## Понимание результатов

Ниже описан формат CLI-вывода. В GUI аналогичные данные доступны в двух режимах (`Таблица`/`Карточки`) на вкладке `Сканирование`.

### Таблица результатов

Каждая строка содержит информацию об одном устройстве:

| Колонка | Описание |
|---------|----------|
| **IP** | IP адрес устройства |
| **MAC** | MAC адрес (может быть пустым) |
| **Hostname** | Имя хоста (если доступно) |
| **Порты** | Список открытых портов с протоколом и сервисом |
| **Протоколы** | Используемые протоколы |
| **Тип устройства** | Определенный тип устройства |
| **Производитель** | Производитель по MAC адресу |
| **ОС (оценка)** | Вероятная ОС + confidence + краткое обоснование (`reason`) |

### Аналитика

После таблицы выводится аналитика:

#### Протоколы в сети

Показывает какие протоколы используются и на скольких устройствах.

#### Используемые порты

Статистика по портам:
- Сколько устройств используют каждый порт
- Назначение порта
- Название сервиса

#### Типы устройств

Распределение устройств по типам.

#### Общая статистика

- Всего устройств
- Устройств с открытыми портами
- Всего открытых портов
- Уникальных протоколов и портов

---

## Устранение неполадок

### Проблема: "Не удалось определить локальную сеть"

**Решение:**
```bash
# Укажите сеть вручную
./network-scanner --network 192.168.1.0/24
```

### Проблема: MAC адреса не определяются

**Причины:**
- Нет прав администратора
- Устройства не отвечают на ARP запросы
- Сетевая конфигурация блокирует ARP

**Решение:**
- Запустите с правами администратора:
  ```bash
  sudo ./network-scanner  # Linux/macOS
  ```
- Это нормально, остальная функциональность работает

### Проблема: Устройства не обнаруживаются

**Возможные причины:**
- Устройства не отвечают на проверяемые порты
- Firewall блокирует подключения
- Устройства в другой подсети

**Решение:**
- Увеличьте таймаут: `--timeout 10`
- Проверьте firewall настройки
- Убедитесь, что устройства в той же сети

### Проблема: Сканирование слишком медленное

**Решение:**
```bash
# Уменьшите диапазон портов
./network-scanner --ports 80,443,22

# Увеличьте количество потоков
./network-scanner --threads 200

# Уменьшите таймаут (осторожно, может пропустить устройства)
./network-scanner --timeout 1
```

### Проблема: GUI искажается в оконном режиме (часть кнопок/панелей не видна)

**Симптомы:**
- элементы в строках интерфейса выглядят "сжатыми";
- часть панели результатов/инструментов видна только в fullscreen;
- в `Host Details` мало места для текста и кнопок.

**Решение:**
- Увеличьте ширину окна минимум до диапазона `1366+` по горизонтали.
- На Windows с высоким DPI попробуйте `125%` вместо `150-200%` для диагностики.
- Проверьте в обоих режимах (оконный и fullscreen): функционал должен быть одинаковым.
- Если проблема воспроизводится, приложите разрешение экрана + DPI + скриншот вкладки.

### Проблема: Слишком много ложных срабатываний

**Решение:**
```bash
# Увеличьте таймаут для более точного определения
./network-scanner --timeout 5
```

### Проблема: Device Control возвращает ошибку `unsupported vendor adapter`

**Причина:** указан неподдерживаемый профиль API устройства.

**Решение:**
- Используйте `--device-vendor generic-http` для endpoint вида `/api/status` и `/api/reboot`.
- Используйте `--device-vendor tp-link-http` для endpoint вида `/api/system/status` и `/api/system/reboot`.

### Проблема: Reboot не запускается

**Причина:** не передано обязательное подтверждение опасной операции.

**Решение:**
```bash
./network-scanner --device-action reboot ... --device-confirm I_UNDERSTAND
```

### Проблема: Не появляются findings в Risk Signatures

**Причина:** сигнатуры сопоставляются только с фактическими данными скана (порты/сервисы/banner/тип устройства).

**Решение:**
- Включите `--grab-banners` для более точного сопоставления.
- Убедитесь, что в скане есть релевантные открытые сервисы (например, `telnet`, `ftp`, `mqtt`, `upnp`).
- Запустите на том же наборе данных: `--risk-signatures`.

### Troubleshooting по типам ошибок инструментов (P3)

| Код ошибки | Что означает | Что сделать |
|---|---|---|
| `not_installed` | Системная утилита отсутствует в `PATH` | Установите утилиту (`ping`/`traceroute`/`whois`) и проверьте `PATH` |
| `permission_denied` | Недостаточно прав для запуска/сетевой операции | Запустите от администратора (Windows) или с `sudo` (Linux/macOS) |
| `timeout` | Превышен таймаут выполнения | Увеличьте `--tool-timeout` (инструменты) или `--timeout` (сканер) |
| `network_error` | Ошибка сети/маршрутизации/DNS | Проверьте доступность хоста, firewall, корректность DNS/маршрута |
| `unknown` | Нестандартная ошибка выполнения | Запустите с `--raw`, сохраните вывод и приложите в issue |

### Платформенные рекомендации (Windows/macOS/Linux)

- **Windows**
  - Для tool-режимов используется `ping`/`tracert`.
  - При `permission_denied` перезапустите терминал или GUI "Run as Administrator".
- **macOS**
  - Для traceroute используется системная `traceroute`.
  - Для некоторых сетевых сценариев (ARP/MAC) может потребоваться запуск с правами.
- **Linux**
  - Для tool-режимов требуется наличие `ping`/`traceroute` в `PATH`.
  - В окружениях с жестким firewall увеличьте таймауты и уменьшите `--threads`.

### Большие подсети и параметры потоков

- Для крупных диапазонов (например, `/23` и больше) GUI покажет подтверждение перед стартом.
- Параметр `threads` валидируется и ограничивается безопасным диапазоном `1..512`.
- В GUI доступен переключатель `Автопрофиль сканирования (рекомендуется)`:
  - при включении приложение автоматически смягчает параметры (`ports`, `threads`) для крупных подсетей;
  - пороги автопрофиля: около `512`, `1024` и `2048` хостов.
- Если требуется полный ручной контроль параметров, отключите автопрофиль перед запуском.
- Для снижения нагрузки начните с:
  - `--threads 50..128`
  - `--timeout 1..2`
  - узкого диапазона портов (`--ports 22,80,443`)

---

## Часто задаваемые вопросы

### Q: Можно ли сканировать UDP порты?

**A:** Да. Используйте флаг `--udp`. По умолчанию приложение сканирует TCP; с `--udp` добавляется проверка популярных UDP-портов.

### Q: Нужны ли права администратора?

**A:** Для получения MAC адресов — да, для остального функционала — нет.

### Q: Можно ли сохранить результаты в файл?

**A:** Используйте перенаправление вывода:
```bash
./network-scanner > results.txt
```

### Q: Как сканировать только определенные устройства?

**A:** Укажите конкретную подсеть или используйте фильтрацию результатов.

### Q: Влияет ли сканирование на производительность сети?

**A:** Да, но обычно минимально. Уменьшите `--threads`, если сеть перегружена.

### Q: Можно ли использовать в корпоративной сети?

**A:** Убедитесь, что у вас есть разрешение на сканирование сети. Некоторые корпоративные политики запрещают сетевые сканеры.

### Q: Как интерпретировать результаты?

**A:** 
- Открытые порты показывают доступные сервисы
- Тип устройства помогает понять назначение устройства
- Протоколы показывают используемые технологии
- Аналитика помогает понять общую картину сети

---

## Дополнительные ресурсы

- [Инструкция по эксплуатации](../Инструкция%20по%20эксплуатации.md) - Полная инструкция по эксплуатации (русский язык)
- [Техническая документация](TECHNICAL.md) - Техническая документация для разработчиков
- [Архитектура проекта](ARCHITECTURE.md) - Описание архитектуры проекта
- [Инструкция по установке](INSTALL.md) - Инструкции по установке для разных платформ
- [README.md](../README.md) - Основная документация проекта
- [Карта разработки](../DEVELOPMENT_MAP.md) - Детальная карта разработки проекта

---

**Версия документа:** 1.0.5  
**Последнее обновление:** 2026-04-23

