# Binaries (только в корне репозитория — не скрывать cmd/network-scanner и internal/gui)
/network-scanner
/network-scanner.exe
/network-scanner-*
/network-scanner-gui
/network-scanner-gui.exe
/network-scanner-gui-*
/gui
/gui.exe

# Build outputs
dist/
Release/
release/
build/

# Local test artifacts
internal/ports/test.zip

# CLI security report auto-names often land in repo root during manual runs
/security-report-redacted-*.html
/security-report-unredacted-*.html

# Go
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out
go.work

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Local environment
.env
.env.*

# Тестовые артефакты: тесты internal/api открывают inventory по относительному
# пути из DefaultConfig(), поэтому БД создаётся внутри пакета.
internal/api/inventory.db
internal/api/nonexistent/

# Артефакты покрытия/тестов (цикл 2026-09-15: предотвращение повторного накопления
# в корне и internal/legacy — см. docs/FINAL_PLAN_2026-09-15.md, задача F4/F5)
*_cov
/*_cov_*
/.tmp/
/*.log
/update_plan.py

# Local lint/test scratch artifacts (never commit)
/.tmp_*
/.lint-issues*.txt
/.test-*.log
